<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Blog</title>
</head>
<body>
    <%- include("_navbar") %>

    <!-- <% posts.reverse() %> -->

    <!-- <% blogs.forEach(posts => { %>  -->
    <div class="container">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title"><%= blogPost.title %></h5>
          <div class="card-body"><%= blogPost.body %></div>
          <p class="text-muted"><%= blogPost.postedAt %></p>
          <a href="/blog/<%= blogPost._id %>" class="btn btn-secondary">Read More</a>
        </div>
        </div>
    </div>
    <!-- <% }) %>   -->
</body>
</html>